import { UserValue } from './user-value';

/**
 * 用户目标数值模型
 */
export class UserTargetValue extends UserValue {
    /**
     * 编号
     */
    public no: number;
    /**
     * 用户ID
     */
    public userID: string;
}